> 터미널에 실행

``` sh
launchctl setenv MAVEN_USERNAME ""
launchctl setenv MAVEN_PASSWORD ""
launchctl setenv DB_URL "jdbc:mysql://localhost:3306"
launchctl setenv DB_USER "root"
launchctl setenv DB_PASS "mysqlpw"
```
